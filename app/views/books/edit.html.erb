<h2>Editing Book</h2>

<%= form_with model: @book do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  
  <p>Opinion</p>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  
  <%= link_to 'show', book_path(@book.id) %>
  |
  <%= link_to 'back', '/books' %>
  
<% end %>